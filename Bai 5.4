#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


void check (const vector<int>& arr , const vector<int>& Q){
    for (int q : Q){
        auto b  = lower_bound(arr.begin() , arr.end() , q);
        if (b != arr.end() && *b == q){
            cout <<"Yes"<<" "<< b - arr.begin() +1 << endl ; 
        }
        else {
            cout <<"No"<<" "<< b - arr.begin() + 1 << endl ; 
        }
    }
}
int main() { 
    int n ; 
    cin >> n ; 
    vector<int> arr(n);
    for (int i = 0 ; i < n ; i++){
        cin >> arr[i];
    }
    int querry ;  
    cin >> querry ; 
    vector<int> Q(querry);
    for (int i = 0 ; i < querry ; i++){
        cin >> Q[i];
    }
    check(arr , Q);
    return 0 ; 
}
