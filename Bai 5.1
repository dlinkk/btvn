#include <stdio.h>
#include <algorithm>
#include <iostream>

using namespace std ;

void update(int *a,int *b) {
       int a1 = *a + *b ; 
       int b1 = abs(*a - *b);
       cout << a1 <<endl ; 
       cout << b1 << endl ; 
}

int main() {
    int a, b;
    cin >> a >> b ; 
    int *pa = &a, *pb = &b;
    update (pa , pb);
    return 0 ; 
}
